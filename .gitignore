# Secrets and local config (NEVER commit — use .env locally, Streamlit Cloud UI only)
.env
.env.*
!.env.example
.env.local
*.pem
*.p12
service-account*.json

.streamlit/secrets.toml
.streamlit/secrets_export.txt
**/*secrets_export*
**/secrets.toml
!**/.streamlit/secrets.toml.example
!**/*secrets.toml.example

# Google OAuth — never inside the repo (use %LOCALAPPDATA%\groww-insights\)
**/credentials.json
**/token.json
**/.oauth_client_id
**/oauth_authorize_url.txt

# MCP server clone (local only — not tracked; OAuth stays outside repo)
MCPServer/saksham-mcp-server/

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.pytest-tmp*/

# IDE / OS
.cursor/
.kiro/
.DS_Store
Thumbs.db

# Debug logs
debug-*.log

# Local DB (optional — remove this line if you want to commit reviews.db)
data/reviews.db

# Generated test artifacts
phase-*/test-results/
**/test-results/
